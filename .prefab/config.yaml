#Configuration for the prefab cli

IMAGE_REPO:
  default: prefab
  prompt: "Repository name for Image"

SERVICE_NAME:
  default: prefab-spring-boot
  prompt: "Service name"

SERVICE_PORT: 
  default: 8080
  prompt: "Service port"

MONITOR_PORT: 
  default: 8081
  prompt: "Service monitor port"